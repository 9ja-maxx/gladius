# GenLayer Testing configuration (gltest)
# Tells the test runner where contracts and artifacts are stored,
# what environment variables file to read, and defaults for chain networks.

paths:
  contracts: contracts
  artifacts: artifacts

environment: .env

networks:
  default: localnet
  localnet:
  testnet_bradbury:
    accounts:
      - "${ACCOUNT_PRIVATE_KEY}"
